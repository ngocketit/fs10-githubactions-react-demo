name: Demo CI/CD node 16

on: [push, pull_request]

jobs:
  node-demo:
    runs-on: ubuntu-18.04
    steps:
      - name: Install node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Echo something
        run: node --version
